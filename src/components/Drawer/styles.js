import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  logOutContainer: {
    flexDirection: 'row',
    alignItems: 'center',
    marginHorizontal: 20,
  },
  logOut: {
    fontWeight: 'bold',
    color: '#777',
  },
});

export default styles;
